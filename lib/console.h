#ifndef CONSOLE_H
#define CONSOLE_H
#include "leader.h"

void Print_Leader(struct Lenta *[], int eilutes, char [], int);
void PrintQuestion(struct A *current, int i);
int Starting_Screen(int pasirinkimas);

#endif